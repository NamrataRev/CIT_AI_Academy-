source "https://rubygems.org"

# Matches the gem set GitHub Pages uses to build the site, so a local
# `bundle exec jekyll serve` mirrors the deployed build.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
